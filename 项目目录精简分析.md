# 项目目录与代码精简分析

> 分析范围：Python 后端 + `gpui-app` GPUI 前端
>
> 目标：在方便同步上游代码的前提下，识别可以删除、合并或延后清理的目录与代码。
>
> 说明：下文的分析阶段仅进行了只读检查；后续执行状态见下方说明。

> 执行状态（2026-08-28）：已确认并完成 `ui/` 目录淘汰。GPUI 所需的 27 个资源已迁移到 `gpui-app/resources/assets/`，`gpui-app/src/assets.rs` 已解除对 `ui/` 的依赖；Tauri 参考截图脚本及相关说明也已移除。本文其余内容保留为清理前的分析快照。

## 总体结论

项目目前处于“Qt/React/Tauri 向 GPUI 迁移”的中间状态，存在旧前端、迁移兼容层、重复资源和旧构建链并存的情况。

可以精简，但建议分阶段进行，不要现在一次性删除旧目录。当前最重要的清理顺序是：

1. 解除 GPUI 对 `ui/` 目录的资源依赖。
2. 统一静态资源来源，清理重复图片。
3. 将构建和 CI 切换到 GPUI + Python sidecar。
4. 最后整体删除 Qt/Tauri 遗留代码和依赖。

## 当前相对安全的精简对象

以下文件目前没有发现有效的项目引用，风险相对较低：

- `test.cs`：独立的 Avalonia 测试文件。
- `scripts/build_avalonia.ps1`：引用不存在的 `avalonia-ui` 目录。
- `docs/avalonia-ui-shadcn-alignment-plan.md`：Avalonia 历史方案，当前前端已经转向 GPUI。
- `gpui-app/resources/icons/` 中没有被代码或配置引用的多尺寸图标。
- `tauri.md`：只有一条 Tauri 开发命令；如果不再维护 Tauri 参考界面，可以移除。

删除前仍建议使用 `rg` 再确认一次是否有外部脚本、CI 或个人工具引用这些文件。

## 目前不应该直接删除的部分

### `ui/`

现在不能直接删除，原因是 GPUI 仍然依赖其中的资源：

- `gpui-app/src/assets.rs` 使用了指向 `ui/src` 和 `ui/public` 的 `include_bytes!`。
- `gpui-app/scripts/capture_tauri_reference.ps1` 和 `capture_tauri_states.ps1` 仍会启动 Tauri 参考界面。
- 部分 GPUI 页面仍以 `ui/src/pages` 作为视觉和交互参考。

建议先把 GPUI 需要的资源统一迁移到根目录 `assets/` 或 `gpui-app/resources/`，再删除整个 `ui/`。

### `gpui-app/src/ipc/mock.rs`

暂时不能删除。当前 Python RPC 只实现了应用和设备相关的一小部分，任务、队伍、资源、工具箱等 GPUI 页面仍大量依赖 Mock 后端。

后续顺序应为：

1. 补全 Python RPC。
2. 页面切换到真实 RPC。
3. 将 Mock 限制到测试或开发模式。

### `app/` 和 `main.py`

它们是旧的 PySide6/qfluentwidgets 前端，但当前构建链仍然依赖它们：

- `scripts/build.py`
- `main.spec`
- `.github/workflows/reusable-build.yml`
- `README.md`
- `assets/doc/zh/build_guide.md`
- `assets/doc/en/build_guide.md`
- `updater.py` 和 `updater.spec`

在 GPUI 的发布、打包和更新流程切换完成前，不建议删除。

## GPUI 切换完成后可整体清理的内容

确认不再发布旧 Qt 前端后，可以考虑一起删除：

- `app/`
- `main.py`
- `main_dev.py`
- `main.spec`
- Qt 专用的构建和开发代码
- 根目录 `i18n/*.ts` 及 Qt 翻译脚本
- `pyproject.toml` 中的 `[tool.pyside6-project]`

同时可以移除只服务旧 Qt 前端的依赖：

- `pyside6`
- `pyside6-fluent-widgets`
- `pysidesix-frameless-window`
- `pyuac`
- `watchdog`

注意：`markdown-it` 仍被 `module/update/checker.py` 使用，不能仅因为删除 Qt 前端而移除。

## 可重点清理的资源

已发现多处资源重复：

- `ui/public/favicon.ico` 与 `assets/logo/my_icon_256X256.ico`
- `ui/public/logo.png`、`ui/src/assets/logo.png` 与 `assets/logo/my_icon.png`
- `ui/public/limbus_title_banner.png`、`ui/src/assets/limbus_title_banner.png` 与 `assets/logo/limbus_title_banner.png`
- `ui/public/sinners/*.png` 与 `assets/app/sinner/*/*_ID_Photo_3.png`
- `ui/public/status_effects/*.png` 与 `assets/app/status_effects/*.png`

旧 Qt 专用资源体积较大：

- `assets/app/fonts`：约 23.9 MB
- `assets/app/theme_packs`：约 36.6 MB
- `assets/app/svgs`：仅旧 Qt 代码使用

这些资源应在确认 GPUI 和发布流程不再使用后清理。建议保留后端运行所需的：

- `assets/images/`
- `assets/model/`
- `assets/minitouch/`
- `assets/config/`
- `assets/binary/`
- `assets/audio/`

## 建议保留的核心目录

这些目录目前属于后端或公共能力，不建议因为移除旧 GUI 而删除：

```text
core/
module/
tasks/
utils/
assets/images/
assets/model/
assets/minitouch/
assets/config/
main_backend.py
gpui-app/
```

其中：

- `core/` 是框架无关的事件和国际化代码。
- `module/`、`tasks/`、`utils/` 没有实际的 Qt 运行时依赖。
- `module/update/checker.py` 和 `module/resource_sync/` 后续仍可能作为 GPUI RPC 后端使用。
- `main_backend.py` 是 GPUI 使用的 Python sidecar 入口，不是旧 Qt 启动器。

## GPUI 自身的代码精简方向

`gpui-app` 中仍存在迁移兼容层：

- `gpui-app/src/components/mod.rs` 同时提供短名称组件 API 和显式 palette API。
- 多个模块使用较宽泛的 `#![allow(dead_code)]` 或 `#![allow(unused_imports)]`。
- 页面目前仍使用部分兼容构造函数。

现在不建议直接删除这些函数。等页面统一后，可以：

1. 删除不再使用的兼容构造函数。
2. 将仅供组件内部使用的函数改为私有。
3. 把模块级 warning 忽略改成逐项处理。
4. 运行 Clippy 和未使用代码检查，按实际引用删除。

以下文件虽然当前是未跟踪文件，但已经被代码使用，不应当当作垃圾文件删除：

- `gpui-app/src/app_inputs.rs`
- `gpui-app/src/components/action_button.rs`
- `gpui-app/src/components/keyboard.rs`

## 更适合同步上游的目录策略

建议长期保持下面的边界：

```text
项目根目录/
├─ core/                 # 公共、框架无关代码
├─ module/               # Python 后端服务
├─ tasks/                # 自动化任务
├─ utils/
├─ main_backend.py       # GPUI 使用的 Python sidecar
├─ assets/               # 后端和公共资源
├─ gpui-app/             # GPUI 前端
├─ scripts/              # 构建和维护脚本
└─ tests/
```

同步上游时建议：

- 不要继续移动 `module/`、`tasks/`、`utils/` 和 `assets/images/`。
- GPUI 代码尽量集中在 `gpui-app/`。
- 后端新接口尽量以新增模块或小范围修改的方式放在 `module/`。
- 旧 Qt 删除单独做一个提交，不要和 GPUI 功能开发混在一起。
- 当前已有大量 GPUI 未提交修改，开始清理前应先做一次 checkpoint 或提交。

当前仓库只配置了 `origin` 远程，并没有单独命名为 `upstream` 的远程。如果确实需要跟踪另一个上游仓库，后续可以单独配置远程并采用专门的同步分支。

## 推荐的迁移/清理阶段

### 阶段一：低风险清理

处理无引用的 Avalonia 实验文件、失效脚本和未使用的 GPUI 图标，并保留完整记录。

### 阶段二：统一 GPUI 资源

选择唯一资源源，修改 `gpui-app/src/assets.rs` 的路径依赖，移除 `ui/` 资源引用和重复图片。

### 阶段三：迁移构建发布

将 `scripts/build.py`、CI、安装包和更新流程改为 GPUI + `main_backend.py`，再决定 `updater.py` 是否保留。

### 阶段四：移除旧 Qt/Tauri

删除 `app/`、旧启动器、Qt 依赖、Qt 翻译链和 Tauri 前端。

### 阶段五：清理迁移兼容层

在真实 RPC 全部接通后，删除 Mock 运行时依赖、旧组件兼容 API 和宽泛的 dead-code 忽略。

## 检查结果

只读验证结果：

- Rust：`cargo +nightly check --manifest-path gpui-app/Cargo.toml` 通过。
- Rust 测试：85 个通过，0 个失败。
- Python 测试：23 个通过，0 个失败。

这说明当前结构主要是迁移收尾和依赖清理问题，并不是必须先修复的编译性问题。

## Qt → GPUI 全量迁移方案

### 总体目标

当前项目已经具备迁移基础：

- GPUI 页面基本完成，Rust 测试 85 个通过。
- Python headless sidecar 已存在，Python 测试 23 个通过。
- 目前真实后端主要只支持设备发现/连接，其余页面仍依赖 `MockClient`。
- `app/`、`main.py`、`scripts/build.py` 仍是 Qt/PySide6 体系。

最终架构：

    AALC.exe（GPUI：窗口、页面、托盘、交互）
            │
            │ Loopback WebSocket + JSON-RPC 2.0 + Token
            ▼
    AALC Backend.exe（Python：配置、设备、任务、工具、同步、更新）

Python 只提供无界面的后端服务，不创建窗口、Qt 对象或 UI 控件。

### 一、固化 Python 后端边界

保持 `main_backend.py` 为纯启动器，新增统一的 `BackendApplication`，负责配置、主题包、设备、任务执行、队伍、工具、资源同步、更新、全局热键、日志事件以及进程退出管理。

`rpc_dispatcher.py` 只负责协议校验和路由，业务逻辑放到后端服务中，并使用统一的锁或串行执行器处理状态变更。

继续使用现有 RPC 方法：

    app.ping/version/checkUpdate/shutdown
    tasks.getConfig/setConfig
    execution.getState/start/stop/pause/resume
    team.list/save/delete
    sinner.list
    themePack.list/updateAll/resetWeights
    resource.status/checkUpdate/sync.start
    tool.start/stop/screenshot
    hotkey.get/set
    systemSettings.get/set
    device.list/connect/disconnect

长耗时操作立即返回 `accepted + runId`，最终结果通过 `execution.status`、`execution.mirrorProgress`、`device.status`、`tool.status`、`resource.sync.progress`、`screenshot.frame`、`log.entry` 和 `app.notice` 推送。

### 二、统一数据所有权和兼容策略

- `config.yaml` 继续由 Python 作为业务配置唯一来源。
- 队伍配置继续兼容旧版 `TeamSetting`。
- 主题包及权重文件格式保持不变。
- `tasks.getConfig` 返回完整可编辑配置，`tasks.setConfig` 使用 patch/merge，不能覆盖未识别字段。
- `team.save` 采用相同的后端合并策略，不能因为 GPUI DTO 不完整而丢失旧字段。
- Rust 的 `AppSettings` 只保存界面布局、语言、主题、侧栏等 UI 偏好。
- GPUI 不直接读写 YAML，所有业务配置通过 RPC 访问。

`TaskConfig`、`TeamConfig`、`SystemSettings` 等 DTO 增加 `schemaVersion`；长任务响应增加 `runId`。保留现有方法名和兼容字段，避免破坏旧配置和更新流程。

### 三、GPUI 脱离 Mock

将页面依赖从 `MockClient` 改为统一的 `BackendClientHandle`：

- 支持异步请求和回调，所有 RPC 请求在 GPUI 后台线程执行。
- 全局只保留一个事件泵，再分发给各页面状态。
- 生产模式连接失败时显示错误并支持重连，禁止静默回退到 Mock。
- `AHAB_BACKEND=mock` 只用于测试和视觉开发。
- Mock 后端实现与真实后端相同的接口。

当前 `WebSocketClient` 的同步 `request()` 需要封装成异步 UI 调用，否则资源同步、更新、截图和任务控制会导致界面卡顿。

### 四、从 Qt 中抽取业务逻辑

暂时不要直接删除 `app/`，先迁移其中仍有业务价值的部分：

- `farming_interface.py`：改造成 Python `ExecutionService`。
- `app/tools_windows/`：抽取无限战斗、截图、理智工具的纯 Python 逻辑，移除 Qt 窗口和 `QTimer`。
- `team_setting_card.py`：只保留字段映射和兼容逻辑，编辑界面完全由 GPUI 实现。
- `theme_pack_setting_interface.py`：将主题包操作接入后端服务。
- `resource_sync_coordinator.py`：改成 RPC 适配层，复用现有纯 Python 资源同步服务。
- `update_ui.py`：移除 Qt 对话框，改成状态和事件接口。
- `hotkey_listener.py`：保留在 Python 后端，由后端控制任务暂停、恢复和停止。

任务停止机制逐步改为 `threading.Event` 协作式取消；`TerminateThread` 只能作为异常兜底，不能作为正常停止路径。

### 五、GPUI 原生系统能力

用 Rust/Win32 替代 Qt/Tauri 的系统功能：

- 托盘图标、显示/隐藏窗口、最小化到托盘。
- 单实例检测和二次启动参数转发。
- 窗口激活、DPI 和前置显示。
- 保持旧版启动参数和托盘菜单行为。
- 旧版管理员权限行为作为兼容项保留，使用 Rust manifest/UAC，不再依赖 `pyuac`。
- Python 负责全局热键和业务动作，Rust 负责窗口与托盘。

### 六、资源和打包

将 GPUI 当前对 `ui/src`、`ui/public` 的资源依赖迁出：

- `assets/`：后端配置、模型、图片和动态资源。
- `gpui-app/resources/`：GPUI 图标、帮助文档和内置静态资源。
- 发布包不能依赖 Node、WebView2 或 Tauri。

重写 `scripts/build.py`，构建流程改为：

    cargo +nightly build --release
    PyInstaller main_backend.spec
    PyInstaller updater.spec
    组装 AALC.exe、AALC Backend.exe、AALC Updater.exe
    复制 assets / resources / README / LICENSE
    生成 AALC_<version>.7z

正式包应包含 GPUI 主程序、Python backend、updater、`assets/`、`resources/`、README 和 LICENSE；CI 增加 Rust nightly、Cargo 缓存、GPUI 构建和 sidecar 启动测试。

### 七、实施阶段

1. **基线与冻结**：保存当前 GPUI 未提交改动，确认现有测试作为基线。
2. **协议与后端服务化**：完成 DTO、配置兼容、`BackendApplication`、事件总线和 RPC 调度，确保 backend 不导入 Qt。
3. **真实功能接入**：依次接入任务执行、设备、队伍、主题包、工具、资源同步、更新、热键和系统设置。
4. **GPUI 全页面接入**：替换所有生产路径中的 `MockClient`，完成异步 RPC、事件泵、错误提示、重连和状态恢复。
5. **系统壳与正式发布**：完成托盘、单实例、权限、资源归档、打包、CI、升级器和文档。
6. **删除旧体系**：发布包验收通过后，再删除 `main.py`、Qt `app/`、Qt 依赖、旧翻译体系、Tauri 前端和无引用资源。

### 八、测试与验收标准

- Python：所有 RPC 方法、参数校验、错误码、配置/队伍兼容、任务状态机、取消、设备、工具、同步和更新。
- Sidecar 集成：token 握手、并发请求、事件顺序、重连、父进程退出和优雅关闭。
- Rust：DTO 序列化、异步客户端、事件 reducer、Mock 后端隔离、页面无同步网络调用。
- Windows E2E：真实游戏窗口、MuMu、ADB、开始/暂停/恢复/停止、托盘、热键、截图、资源同步和更新。
- 发布包：在没有 Python、Qt、WebView2、Node 的干净机器上能够启动。
- 配置验收：旧版 `config.yaml`、队伍配置、主题包及权重文件升级后不丢失。

最终标准：生产模式不使用 Mock，所有旧版用户可见功能均由 GPUI + Python backend 完成。

### 默认假设

- 目标平台仍为 Windows x64。
- 保留旧版全部用户功能，包括托盘、热键、更新、资源同步和工具。
- 保留旧配置文件兼容性。
- Python 后端以独立 sidecar 进程发布。
- Rust GPUI 负责窗口、托盘和单实例，Python 负责业务执行和全局热键。
- `ui/` 和 Qt 代码只在迁移完成并通过发布包验收后删除。
